function setup() {
    createCanvas(940, 800); 
    background('white');
  }
  
  function draw() {
    
    
    stroke ('black');
    fill ('purple')
    
    //console.log (mouseIsPressed);
    
    if (mouseIsPressed) {
      
      rect (mouseX, mouseY, 40, 40);}
    }
    